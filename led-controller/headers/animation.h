#pragma once

/* Initialize the animation module. */
void init_animation();

/* Update the animation based on the current configuration. */
void update_animation();

/* Advance the animation by one tick. */
void advance_animation();
